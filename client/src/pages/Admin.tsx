import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { formatCurrency } from "@/data/catalog";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, Loader2, PackagePlus, Save, Upload, X } from "lucide-react";
import { ChangeEvent, useMemo, useState } from "react";
import { toast } from "sonner";

type ProductKind = "ebook" | "course";
type ProductStatus = "draft" | "published" | "archived";

type ProductForm = {
  slug: string; productType: ProductKind; status: ProductStatus; title: string; subtitle: string; description: string; category: string; coverUrl: string; coverKey: string; accent: "lime" | "violet" | "rose" | "cyan"; priceBaht: string; unitCount: string; durationLabel: string; content: string;
};

const ebookTemplate = JSON.stringify({ kind: "ebook", intro: "เกริ่นนำเนื้อหาของหนังสือ", chapters: [{ kicker: "บทที่ 01", title: "ชื่อบทแรก", body: ["ย่อหน้าแรกของเนื้อหา", "ย่อหน้าที่สองของเนื้อหา"] }] }, null, 2);
const courseTemplate = JSON.stringify({ kind: "course", intro: "เกริ่นนำคอร์สของคุณ", modules: [{ title: "บทเรียนที่ 1", duration: "30 นาที", summary: "สิ่งที่ผู้เรียนจะได้จากบทเรียนนี้" }] }, null, 2);

const emptyForm = (productType: ProductKind = "ebook"): ProductForm => ({ slug: "", productType, status: "draft", title: "", subtitle: "", description: "", category: "", coverUrl: "", coverKey: "", accent: "lime", priceBaht: "", unitCount: "1", durationLabel: "", content: productType === "ebook" ? ebookTemplate : courseTemplate });

function recordToForm(product: { slug: string; productType: ProductKind; status: ProductStatus; title: string; subtitle: string | null; description: string; category: string; coverUrl: string; coverKey: string | null; accent: "lime" | "violet" | "rose" | "cyan"; priceSatang: number; unitCount: number; durationLabel: string; content: string }): ProductForm {
  return { slug: product.slug, productType: product.productType, status: product.status, title: product.title, subtitle: product.subtitle ?? "", description: product.description, category: product.category, coverUrl: product.coverUrl, coverKey: product.coverKey ?? "", accent: product.accent, priceBaht: String(product.priceSatang / 100), unitCount: String(product.unitCount), durationLabel: product.durationLabel, content: product.content };
}

export default function Admin() {
  const { user } = useAuth({ redirectOnUnauthenticated: true });
  const utils = trpc.useUtils();
  const products = trpc.admin.listProducts.useQuery(undefined, { enabled: user?.role === "admin" });
  const orders = trpc.admin.listOrders.useQuery(undefined, { enabled: user?.role === "admin" });
  const [activeTab, setActiveTab] = useState<"products" | "orders">("products");
  const [editingSlug, setEditingSlug] = useState<string | null>(null);
  const [form, setForm] = useState<ProductForm>(() => emptyForm());

  const productMap = useMemo(() => new Map((products.data ?? []).map((product) => [product.slug, product])), [products.data]);
  const saveProduct = trpc.admin.createProduct.useMutation({ onSuccess: async () => { await utils.admin.listProducts.invalidate(); setEditingSlug(null); setForm(emptyForm()); toast.success("บันทึกสินค้าแล้ว"); }, onError: (error) => toast.error("บันทึกสินค้าไม่สำเร็จ", { description: error.message }) });
  const updateProduct = trpc.admin.updateProduct.useMutation({ onSuccess: async () => { await utils.admin.listProducts.invalidate(); toast.success("อัปเดตสินค้าแล้ว"); }, onError: (error) => toast.error("อัปเดตสินค้าไม่สำเร็จ", { description: error.message }) });
  const uploadCover = trpc.admin.uploadCover.useMutation({ onSuccess: ({ url, key }) => { setForm((current) => ({ ...current, coverUrl: url, coverKey: key })); toast.success("อัปโหลดรูปปกแล้ว"); }, onError: (error) => toast.error("อัปโหลดรูปปกไม่สำเร็จ", { description: error.message }) });

  const patch = (value: Partial<ProductForm>) => setForm((current) => ({ ...current, ...value }));
  const handleCoverFile = (event: ChangeEvent<HTMLInputElement>) => {
    const file = event.target.files?.[0];
    if (!file) return;
    if (!/image\/(png|jpeg|webp)/.test(file.type) || file.size > 3 * 1024 * 1024) { toast.error("ใช้ได้เฉพาะ PNG, JPG หรือ WebP ขนาดไม่เกิน 3 MB"); event.target.value = ""; return; }
    const reader = new FileReader();
    reader.onload = () => uploadCover.mutate({ dataUrl: String(reader.result), filename: file.name });
    reader.readAsDataURL(file);
  };
  const submit = () => {
    const payload = { slug: form.slug.trim(), productType: form.productType, status: form.status, title: form.title.trim(), subtitle: form.subtitle.trim() || null, description: form.description.trim(), category: form.category.trim(), coverUrl: form.coverUrl.trim(), coverKey: form.coverKey.trim() || null, accent: form.accent, priceSatang: Math.round(Number(form.priceBaht) * 100), currency: "thb" as const, unitCount: Number(form.unitCount), durationLabel: form.durationLabel.trim(), content: form.content.trim() };
    if (editingSlug) updateProduct.mutate({ slug: editingSlug, product: payload }); else saveProduct.mutate(payload);
  };
  const openEdit = (slug: string) => { const product = productMap.get(slug); if (!product) return; setEditingSlug(slug); setForm(recordToForm(product)); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const isSaving = saveProduct.isPending || updateProduct.isPending;

  return <DashboardLayout><div className="min-h-screen bg-[#0a0b0d] text-white"><div className="mx-auto max-w-7xl px-4 py-7 sm:px-7"><div className="flex flex-col justify-between gap-5 border-b border-white/10 pb-7 lg:flex-row lg:items-end"><div><div className="eyebrow text-[#d5ff45]">พื้นที่ผู้ดูแล</div><h1 className="font-display mt-3 text-4xl tracking-[-0.06em] sm:text-5xl">จัดการร้านของคุณ</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/56">ตั้งราคาบาทไทย อัปโหลดหน้าปก จัดการ eBook และคอร์ส รวมถึงตรวจสอบสิทธิ์ที่ปลดล็อกหลังชำระเงิน</p></div><div className="flex gap-2"><button onClick={() => setActiveTab("products")} className={`rounded-full px-4 py-2.5 text-[10px] font-bold tracking-[0.1em] ${activeTab === "products" ? "gradient-cta text-black" : "border border-white/13 text-white/70"}`}>สินค้า</button><button onClick={() => setActiveTab("orders")} className={`rounded-full px-4 py-2.5 text-[10px] font-bold tracking-[0.1em] ${activeTab === "orders" ? "gradient-cta text-black" : "border border-white/13 text-white/70"}`}>คำสั่งซื้อ</button></div></div>
    {activeTab === "products" ? <div className="grid gap-8 py-8 xl:grid-cols-[minmax(0,1fr)_420px]"><section className="order-2 xl:order-1"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-display text-3xl tracking-[-0.045em]">สินค้าในร้าน</h2><p className="mt-1 text-xs text-white/48">ราคาและสถานะที่แสดงต่อผู้ซื้อเป็นข้อมูลจากฐานข้อมูล</p></div><button onClick={() => { setEditingSlug(null); setForm(emptyForm()); }} className="inline-flex items-center gap-2 rounded-full border border-white/16 px-4 py-2 text-[10px] font-bold tracking-[0.1em] transition hover:border-white/40"><PackagePlus size={14} /> สร้างสินค้าใหม่</button></div><div className="overflow-hidden rounded-2xl border border-white/10"><div className="grid grid-cols-[1.4fr_.6fr_.55fr_.7fr] gap-3 border-b border-white/10 bg-white/[0.03] px-5 py-3 text-[9px] font-bold tracking-[0.13em] text-white/42"><span>สินค้า</span><span>ประเภท</span><span>สถานะ</span><span className="text-right">จัดการ</span></div>{products.isLoading ? <div className="grid h-48 place-items-center text-sm text-white/42"><Loader2 className="animate-spin" size={18} /></div> : products.data?.map((product) => <div key={product.slug} className="grid grid-cols-[1.4fr_.6fr_.55fr_.7fr] items-center gap-3 border-b border-white/7 px-5 py-4 last:border-0"><div className="flex min-w-0 items-center gap-3"><img src={product.coverUrl} alt="" className="h-11 w-9 rounded-md object-cover" /><div className="min-w-0"><div className="truncate text-sm font-semibold">{product.title}</div><div className="mt-1 font-mono text-[9px] text-white/42">{formatCurrency(product.priceSatang)}</div></div></div><span className="text-xs text-white/62">{product.productType === "ebook" ? "eBook" : "คอร์ส"}</span><span className={`w-fit rounded-full px-2 py-1 text-[9px] font-bold ${product.status === "published" ? "bg-lime-300/14 text-lime-200" : product.status === "draft" ? "bg-amber-300/14 text-amber-200" : "bg-white/10 text-white/52"}`}>{product.status === "published" ? "เผยแพร่" : product.status === "draft" ? "ฉบับร่าง" : "เก็บถาวร"}</span><button onClick={() => openEdit(product.slug)} className="justify-self-end rounded-lg border border-white/12 px-3 py-2 text-[9px] font-bold tracking-[0.08em] text-white/75 transition hover:border-white/35">แก้ไข</button></div>)}</div></section>
    <aside className="order-1 rounded-2xl border border-white/10 bg-white/[0.025] p-5 xl:order-2"><div className="flex items-start justify-between gap-3"><div><div className="eyebrow text-[#d5ff45]">{editingSlug ? "แก้ไขสินค้า" : "สร้างสินค้า"}</div><h2 className="font-display mt-2 text-3xl tracking-[-0.045em]">{editingSlug ? form.title || "สินค้า" : "สินค้าใหม่"}</h2></div>{editingSlug && <button onClick={() => { setEditingSlug(null); setForm(emptyForm()); }} className="grid h-8 w-8 place-items-center rounded-full border border-white/12 text-white/60"><X size={15} /></button>}</div><div className="mt-6 space-y-4"><div className="grid grid-cols-2 gap-3"><label className="admin-field">ประเภท<select value={form.productType} onChange={(e) => patch({ productType: e.target.value as ProductKind, content: e.target.value === "ebook" ? ebookTemplate : courseTemplate })}><option value="ebook">eBook HTML</option><option value="course">คอร์ส</option></select></label><label className="admin-field">สถานะ<select value={form.status} onChange={(e) => patch({ status: e.target.value as ProductStatus })}><option value="draft">ฉบับร่าง</option><option value="published">เผยแพร่</option><option value="archived">เก็บถาวร</option></select></label></div><label className="admin-field">Slug (ห้ามเปลี่ยนหลังมีผู้ซื้อ)<input value={form.slug} disabled={Boolean(editingSlug)} onChange={(e) => patch({ slug: e.target.value.toLowerCase().replace(/\s+/g, "-") })} placeholder="ชื่อสินค้าภาษาอังกฤษ" /></label><label className="admin-field">ชื่อสินค้า<input value={form.title} onChange={(e) => patch({ title: e.target.value })} placeholder="เช่น ระบบงานที่เบาขึ้น" /></label><label className="admin-field">คำโปรย<input value={form.subtitle} onChange={(e) => patch({ subtitle: e.target.value })} placeholder="คำโปรยสั้น ๆ" /></label><label className="admin-field">รายละเอียด<textarea rows={3} value={form.description} onChange={(e) => patch({ description: e.target.value })} placeholder="อธิบายประโยชน์ของสินค้า" /></label><div className="grid grid-cols-2 gap-3"><label className="admin-field">หมวดหมู่<input value={form.category} onChange={(e) => patch({ category: e.target.value })} /></label><label className="admin-field">จำนวนบท/บทเรียน<input type="number" min="1" value={form.unitCount} onChange={(e) => patch({ unitCount: e.target.value })} /></label></div><div className="grid grid-cols-2 gap-3"><label className="admin-field">ราคา (บาท)<input type="number" min="5" value={form.priceBaht} onChange={(e) => patch({ priceBaht: e.target.value })} /></label><label className="admin-field">ระยะเวลา<input value={form.durationLabel} onChange={(e) => patch({ durationLabel: e.target.value })} placeholder="เช่น 4 ชั่วโมง" /></label></div><label className="admin-field">โทนสี<select value={form.accent} onChange={(e) => patch({ accent: e.target.value as ProductForm["accent"] })}><option value="lime">Lime</option><option value="violet">Violet</option><option value="rose">Rose</option><option value="cyan">Cyan</option></select></label><div className="rounded-xl border border-dashed border-white/16 p-4"><div className="flex items-center justify-between gap-3"><div><div className="text-xs font-bold">รูปหน้าปก</div><p className="mt-1 text-[10px] leading-4 text-white/45">PNG, JPG หรือ WebP ขนาดไม่เกิน 3 MB</p></div><label className="inline-flex cursor-pointer items-center gap-2 rounded-lg border border-white/14 px-3 py-2 text-[9px] font-bold tracking-[0.08em] transition hover:border-white/35"><Upload size={13} /> {uploadCover.isPending ? "กำลังอัปโหลด" : "อัปโหลด"}<input type="file" accept="image/png,image/jpeg,image/webp" className="hidden" onChange={handleCoverFile} /></label></div>{form.coverUrl && <div className="mt-3 flex gap-3"><img src={form.coverUrl} alt="ตัวอย่างรูปปก" className="h-16 w-12 rounded-md object-cover" /><input className="min-w-0 flex-1 rounded-lg border border-white/12 bg-black/20 px-3 text-[10px] text-white/62" value={form.coverUrl} onChange={(e) => patch({ coverUrl: e.target.value })} /></div>}</div><label className="admin-field">เนื้อหาแบบ JSON<textarea className="font-mono text-[10px] leading-5" rows={10} value={form.content} onChange={(e) => patch({ content: e.target.value })} /></label><button disabled={isSaving || uploadCover.isPending} onClick={submit} className="gradient-cta flex w-full items-center justify-center gap-2 rounded-xl px-4 py-3.5 text-[10px] font-extrabold tracking-[0.1em] text-black disabled:opacity-60"><Save size={14} /> {isSaving ? "กำลังบันทึก…" : editingSlug ? "บันทึกการแก้ไข" : "สร้างสินค้า"}</button></div></aside></div> : <section className="py-8"><div className="mb-5"><div className="eyebrow text-[#d5ff45]">สิทธิ์ที่ได้รับการยืนยันแล้ว</div><h2 className="font-display mt-3 text-4xl tracking-[-0.05em]">คำสั่งซื้อ</h2><p className="mt-2 text-sm text-white/52">แสดงเฉพาะสินค้าที่ Stripe ยืนยันการชำระเงินและปลดล็อกสิทธิ์เรียบร้อยแล้ว</p></div><div className="overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[760px] text-left"><thead className="border-b border-white/10 bg-white/[0.03] text-[9px] font-bold tracking-[0.13em] text-white/42"><tr><th className="px-5 py-3">ผู้ซื้อ</th><th className="px-5 py-3">สินค้า</th><th className="px-5 py-3">วันที่</th><th className="px-5 py-3">สถานะ</th><th className="px-5 py-3">Stripe</th></tr></thead><tbody>{orders.isLoading ? <tr><td colSpan={5} className="px-5 py-12 text-center text-sm text-white/42">กำลังโหลดคำสั่งซื้อ…</td></tr> : orders.data?.length ? orders.data.map((order) => <tr key={order.id} className="border-b border-white/7 last:border-0"><td className="px-5 py-4"><div className="text-sm font-semibold">{order.buyerName ?? "ผู้ซื้อ"}</div><div className="mt-1 text-xs text-white/43">{order.buyerEmail ?? "ไม่ระบุอีเมล"}</div></td><td className="px-5 py-4 text-sm text-white/76">{productMap.get(order.productId)?.title ?? order.productId}</td><td className="px-5 py-4 text-xs text-white/54">{new Date(order.purchasedAt).toLocaleString("th-TH")}</td><td className="px-5 py-4"><span className="inline-flex items-center gap-1.5 rounded-full bg-lime-300/14 px-2.5 py-1 text-[9px] font-bold text-lime-200"><CheckCircle2 size={12} /> ปลดล็อกแล้ว</span></td><td className="px-5 py-4 font-mono text-[9px] text-white/40">{order.stripeCheckoutSessionId.slice(0, 18)}…</td></tr>) : <tr><td colSpan={5} className="px-5 py-12 text-center text-sm text-white/42">ยังไม่มีคำสั่งซื้อที่ยืนยันแล้ว</td></tr>}</tbody></table></div></section>}</div></div></DashboardLayout>;
}
